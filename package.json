{
  "name": "tiktok-auto-poster",
  "version": "0.1.0",
  "private": true,
  "description": "Pulls due drafts from a content queue and posts them to TikTok at your configured schedule.",
  "type": "commonjs",
  "engines": {
    "node": ">=20.0.0"
  },
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^5.0.5",
    "@google-cloud/firestore": "^8.6.0",
    "@google-cloud/secret-manager": "^6.2.0",
    "@google-cloud/tasks": "^6.2.3"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "/node_modules/"
    ]
  }
}
